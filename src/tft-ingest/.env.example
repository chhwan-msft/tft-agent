# Example .env for tft-ingest
# Copy this file to .env and fill in the values (do NOT commit secrets).

# -----------------------------
# Azure AI Search
# -----------------------------
# Full endpoint URL, e.g. https://<your-search-service>.search.windows.net
AZURE_SEARCH_ENDPOINT=
# Admin (or query) key for the search service
AZURE_SEARCH_ADMIN_KEY=
# Index names used by the project
AZURE_SEARCH_INDEX_UNITS=tftset15-units
AZURE_SEARCH_INDEX_ITEMS=tftset15-items
AZURE_SEARCH_INDEX_TRAITS=tftset15-traits
# API version used for the preview integrated vectorization features
AZURE_SEARCH_API_VERSION=2024-05-01-Preview

# -----------------------------
# Azure Blob Storage
# -----------------------------
# Connection string for the storage account
AZURE_BLOB_CONNECTION_STRING=
# Container names where JSONL files will be uploaded
AZURE_BLOB_CONTAINER_UNITS=tftset15-units
AZURE_BLOB_CONTAINER_ITEMS=tftset15-items
AZURE_BLOB_CONTAINER_TRAITS=tftset15-traits

# -----------------------------
# Azure OpenAI (for embeddings / vectorizer)
# -----------------------------
# OpenAI resource endpoint (if using Azure OpenAI)
AZURE_OPENAI_ENDPOINT=
# API key for the deployment
AZURE_OPENAI_API_KEY=
# Default deployment name for embeddings (example)
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=text-embedding-3-large
# OpenAI API version (adjust if needed)
AZURE_OPENAI_API_VERSION=2023-10-01-preview

# -----------------------------
# CommunityDragon (CDragon) / data sources
# -----------------------------
# Explicit CDragon URLs used by the ingestion scripts (hardcoded for Set 15).
# These point at the raw.communitydragon.org endpoints and can be overridden if needed.
CDRAGON_UNITS_URL=https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/default/v1/tftchampions-teamplanner.json
CDRAGON_ITEMS_URL=https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/default/v1/tftitems.json
CDRAGON_TRAITS_URL=https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/default/v1/tfttraits.json
# Optional: set identifier (15) or patch tag used to build other CDragon URLs
CDRAGON_SET=15

# -----------------------------
# Misc
# -----------------------------
# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Notes:
# - After filling in this file, run the ingestion steps from the README.
# - Keep secrets out of version control. Use a secrets manager or CI variables when deploying.
