# Example .env for tft-ingest
# Copy this file to .env and fill in the values (do NOT commit secrets).

# -----------------------------
# Azure AI Search
# -----------------------------
# Full endpoint URL, e.g. https://<your-search-service>.search.windows.net
AZURE_SEARCH_ENDPOINT=
# Admin (or query) key for the search service
AZURE_SEARCH_ADMIN_KEY=
# Index names used by the project
AZURE_SEARCH_INDEX_UNITS=tftset15-units
AZURE_SEARCH_INDEX_ITEMS=tftset15-items
AZURE_SEARCH_INDEX_TRAITS=tftset15-traits
# API version used for the preview integrated vectorization features
AZURE_SEARCH_API_VERSION=2024-05-01-Preview

# -----------------------------
# Azure Blob Storage
# -----------------------------
```bash
# Example .env for tft-ingest
# Copy this file to .env and fill in the values (do NOT commit secrets).

# -----------------------------
# Azure Cognitive Search
# -----------------------------
# Full endpoint URL, e.g. https://<your-search-service>.search.windows.net
AZURE_SEARCH_ENDPOINT=
# Admin key for the search service (used for creating indexes/skillsets/indexers)
AZURE_SEARCH_ADMIN_KEY=
# Index names used by the project
AZURE_SEARCH_INDEX_UNITS=tftset15-units
AZURE_SEARCH_INDEX_ITEMS=tftset15-items
AZURE_SEARCH_INDEX_TRAITS=tftset15-traits
# API version used for vector/preview features
AZURE_SEARCH_API_VERSION=2024-05-01-Preview

# Optional: use a managed identity instead of a key
AZURE_MANAGED_IDENTITY_CLIENT_ID=

# -----------------------------
# Azure subscription / storage (used to build blob connection strings)
# -----------------------------
AZURE_SUBSCRIPTION_ID=
AZURE_RESOURCE_GROUP=
AZURE_STORAGE_ACCOUNT=

# Blob containers where JSONL files will be uploaded (used by upload_jsonl/create_blob_datasource)
BLOB_CONTAINER_UNITS=tftset15-units
BLOB_CONTAINER_ITEMS=tftset15-items
BLOB_CONTAINER_TRAITS=tftset15-traits

# -----------------------------
# Azure OpenAI / Azure Foundry (for embeddings / vectorizer)
# -----------------------------
# Resource URI for AOAI (or Foundry equivalent). Do NOT paste secrets here in example.
AOAI_RESOURCE_URI=
# API key for the AOAI embedding deployment (store securely)
AOAI_API_KEY=
# Deployment/model name used for embeddings
AOAI_EMBED_MODELNAME=
# Embedding dimension and metric
AOAI_EMBED_DIM=1536
AOAI_EMBED_METRIC=cosine

# -----------------------------
# Data sources (ingestion)
# -----------------------------
# URLs for the ingestion sources. Leave blank or override locally. Do NOT commit private URLs.
CDRAGON_UNITS_URL=
CDRAGON_ITEMS_URL=
CDRAGON_TRAITS_URL=
# Optional: set identifier (e.g., 15)
CDRAGON_SET=15

# -----------------------------
# Misc
# -----------------------------
# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Notes:
# - After filling in this file, run the ingestion steps from the README.
# - Keep secrets out of version control. Use a secrets manager or CI variables when deploying.

```
